# Claude Handoff Pointer

Read the current continuation handoff first:

```text
claudee.md
```

Then use `HANDOFF.md` only for the longer commerce, Supabase, Stripe, email, admin,
and domain history.

Work from the existing local folder:

```text
/Users/hector/cemi-rum
```

If you are on Hector's Mac, do not start from a fresh GitHub clone. The unfinished
performance work and the latest visual-bug notes are local-only until they are
validated, committed, and deployed.

If you are working on another computer, fetch `origin/main` before editing and
read the mandatory workspace preflight in `claudee.md`. Do not edit or deploy if
your checkout is older than `origin/main`, or if the official SVG logo and footer
`Find Us` checks fail.
